{
  "name": "example",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rm -rf dist && tsc -p ./tsconfig.json",
    "build:verbose": "yarn build && tsconfig-replace-paths -p ./tsconfig.json -v",
    "build:quiet": "yarn build && tsconfig-replace-paths -p ./tsconfig.json -q",
    "build:both": "yarn build && tsconfig-replace-paths -p ./tsconfig.json -v -q"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "tsconfig-replace-paths": "../"
  }
}